// Version.swift — single source of truth for the m11asm version.
//
// Bump on release; the tag is `v` + this string.

/// Semantic version of m11asm / m11asmCore.
public let m11asmVersion = "0.4.0"
